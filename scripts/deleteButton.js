const $ = require("jquery")
const firebase = require("firebase")
const auth = require("./auth/authorization")

// click event to delete tracked movie when selected by user
const DeletButton = function(event){
    $("#deleteTrackedMovie").on(click, event)
    // require data handler info
    const dataHandler = require("./auth/dataHandler")
{


    }

}